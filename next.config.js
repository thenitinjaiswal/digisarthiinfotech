// next.config.js
// Configuration file for Next.js App Router project in pure JavaScript.
// Defines reactStrictMode and experimental features if needed.

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
